name: FlakeShield Runtime Validation
on: [pull_request, push]

jobs:
  validate-action:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install pytest

      - name: Run tests and generate JUnit XML
        run: pytest --junitxml=junit.xml || true 

      - name: Run FlakeShield Action
        uses: deeoli/flakeshield-scratch@feature/config-layer
        with:
          reports: "junit.xml"
          enable_semantic: "true"
          warn_on_high: "true"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload FlakeShield artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: flakeshield-report
          path: |
            outputs/flake_report.json
            outputs/flake_report.md
            outputs/flakeshield.db

      - name: Post PR Comment
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const marker = '';
            const filePath = 'outputs/flake_report.md'; // Use the MD directly generated by the action

            if (!fs.existsSync(filePath)) {
              console.log("No PR summary file found.");
              return;
            }

            const body = fs.readFileSync(filePath, 'utf8');
            const fullBody = marker + "\n" + body;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c => c.body && c.body.startsWith(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body: fullBody,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: fullBody,
              });
            }